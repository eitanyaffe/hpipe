#!/usr/bin/env perl

use strict;
use warnings FATAL => qw(all);
use List::Util qw(sum);
use Getopt::Long;

# title of job
my $title   = "title";

# log directory
my $ldir = 24;

# send email
my $email;

GetOptions ("title=s" => \$length,    # numeric
	    "fn=s"    => \$data,      # string
	    "verbose" => \$verbose)   # flag
    or die("Error in command line arguments\n");
